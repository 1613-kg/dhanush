class UserData {
  String id;
  String userName;
  String profilePic;
  bool isAdmin;
  String email;
  String password;

  UserData(this.id, this.email, this.userName, this.isAdmin, this.password,
      this.profilePic);
}
